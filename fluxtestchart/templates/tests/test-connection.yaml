apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluxtestchart.fullname" . }}-test-connection"
  labels:
{{ include "fluxtestchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fluxtestchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
