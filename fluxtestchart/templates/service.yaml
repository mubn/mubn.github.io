apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxtestchart.fullname" . }}
  labels:
    {{- include "fluxtestchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluxtestchart.selectorLabels" . | nindent 4 }}
